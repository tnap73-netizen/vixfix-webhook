{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"$BMCMC_PROOF_SENTINEL\" = \"railway-cron-proof\" ]; then python3 railway-cron-proof/probe.py; else gunicorn app:app --bind 0.0.0.0:${PORT:-8080} --workers 2 --timeout 30; fi'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
