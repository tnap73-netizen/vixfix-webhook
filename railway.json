{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python3 railway-cron-proof/probe.py",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
